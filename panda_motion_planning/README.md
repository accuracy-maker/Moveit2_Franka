# Panda Motion Planning

After previous step that we finished all `panda_moveit_config` ROS2 package, a natural step is that we want to add some obstacles in the scene.

This ros2 package aims to do that.

## Important Packages
In order to add obstacles into scene, `planning_scene_interface` is the right tool provided by `Moveit` officially.

## Planning around Box

